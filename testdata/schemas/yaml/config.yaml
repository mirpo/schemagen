$schema: http://json-schema.org/draft-07/schema#
title: Config
description: Application configuration (YAML format test)
type: object
properties:
  appName:
    type: string
  version:
    type: string
  settings:
    $ref: "./settings.yaml"
required:
  - appName
  - version
